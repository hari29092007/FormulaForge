import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>FormulaForge • Built for clear reference and fast search.</p>
      <p>Favorites and theme preferences are saved in your browser.</p>
    </footer>
  );
}

export default Footer;
